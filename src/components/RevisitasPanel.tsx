import React,{useEffect,useState}from'react';import{X,ShieldCheck,CalendarDays,CheckCircle2}from'lucide-react';import{api}from'../services/api';
type C={service_id:string;title?:string;contract_number?:string|null};export const RevisitasPanel:React.FC<{onClose:()=>void}>=({onClose})=>{const[items,setItems]=useState<any[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[busy,setBusy]=useState(false),[issue,setIssue]=useState(''),[description,setDescription]=useState(''),[selected,setSelected]=useState<any|null>(null);const load=async()=>{setLoading(true);setError('');try{const cs=await api.get<{contracts:C[]}>('/contracts');const out:any[]=[];for(const c of cs?.contracts||[]){try{const completion=await api.get<any>(`/completion/${c.service_id}`),w=await api.get<any>(`/completion/${c.service_id}/warranty`);const proof=Array.isArray(completion?.photos)?completion.photos.length>0:Boolean(completion?.photos);if(String(completion?.status||'').toUpperCase()==='COMPLETADA'&&completion?.completion_submitted&&proof&&!w?.warranty?.expired)out.push({...c,warranty:w.warranty,revisits:w.revisits||[]});}catch{}}setItems(out)}catch(e:any){setError(e?.message||'No se pudieron cargar los trabajos elegibles.')}finally{setLoading(false)}};useEffect(()=>{void load()},[]);const request=async()=>{if(!selected)return;if(issue.trim().length<3||description.trim().length<10){setError('Indica el problema y una explicación de al menos 10 caracteres.');return}setBusy(true);setError('');try{const r=await api.post<any>(`/completion/${selected.service_id}/warranty/revisit`,{issue:issue.trim(),description:description.trim()});setIssue('');setDescription('');setSelected(null);await load();window.alert(`Revisita solicitada correctamente. Estado guardado: ${r?.status||'SOLICITADA'}.`)}catch(e:any){setError(e?.message||'La solicitud fue rechazada.')}finally{setBusy(false)}};return <div className="fixed inset-0 z-[96] bg-slate-950/70 flex items-center justify-center p-3" onClick={onClose}><div className="w-full max-w-3xl max-h-[92vh] overflow-y-auto rounded-3xl bg-slate-100 shadow-2xl" onClick={e=>e.stopPropagation()}><div className="sticky top-0 z-10 bg-white border-b p-5 flex justify-between"><div><p className="text-[10px] font-black uppercase tracking-widest text-indigo-700">SERVIYA • GARANTÍA</p><h2 className="text-2xl font-black text-slate-900">Revisitas</h2><p className="text-xs text-slate-500 mt-1">Solo trabajos finalizados con evidencia registrada y garantía vigente.</p></div><button onClick={onClose} className="p-2 rounded-xl bg-slate-100"><X/></button></div><div className="p-4 space-y-3">{error&&<div className="rounded-2xl bg-red-50 border border-red-200 text-red-800 p-3 text-xs font-semibold">{error}</div>}{loading?<div className="bg-white rounded-2xl p-8 text-center text-sm text-slate-500">Cargando trabajos elegibles…</div>:!items.length?<div className="bg-white rounded-2xl p-8 text-center"><ShieldCheck className="w-10 h-10 mx-auto text-slate-300"/><p className="font-black text-slate-700 mt-3">No hay trabajos elegibles para una revisita</p><p className="text-xs text-slate-500 mt-1">Debe ser un trabajo ya finalizado, con prueba/evidencia registrada y garantía vigente.</p></div>:items.map(x=><article key={x.service_id} className="bg-white rounded-2xl border p-4"><div className="flex justify-between gap-3"><div><p className="text-[10px] uppercase font-black text-indigo-600">{x.contract_number||'Contrato SERVIYA'}</p><h3 className="font-black text-slate-900">{x.title||x.warranty?.service_title}</h3><p className="text-xs text-slate-500 mt-1">Garantía: {x.warranty?.coverage_days||15} días · vence {x.warranty?.expires_at?new Date(x.warranty.expires_at).toLocaleDateString('es-DO'):'—'}</p></div><span className="px-2 py-1 rounded-lg bg-emerald-50 text-emerald-700 text-[10px] font-black"><CheckCircle2 className="inline w-3 h-3"/> Finalizado</span></div><p className="text-xs text-emerald-700 font-bold mt-3">✓ Evidencia registrada</p>{x.revisits?.length>0&&<div className="mt-3 space-y-2">{x.revisits.map((r:any)=><div key={r.id} className="rounded-xl bg-slate-50 p-3 text-xs"><b>{r.issue}</b><span className="ml-2 text-indigo-700 font-black">{r.status}</span>{r.scheduled_at&&<p className="mt-1 text-blue-700"><CalendarDays className="inline w-3 h-3"/> {new Date(r.scheduled_at).toLocaleString('es-DO')}</p>}</div>)}</div>}<button onClick={()=>{setSelected(x);setError('')}} className="mt-3 w-full rounded-xl bg-indigo-600 text-white py-3 text-sm font-black">Solicitar revisita de este trabajo</button></article>)}</div>{selected&&<div className="fixed inset-0 z-[110] bg-slate-950/60 flex items-center justify-center p-3" onClick={()=>setSelected(null)}><div className="w-full max-w-lg bg-white rounded-3xl p-5" onClick={e=>e.stopPropagation()}><h3 className="text-xl font-black">Solicitar revisita</h3><p className="text-xs text-slate-500 mt-1">{selected.title||selected.warranty?.service_title}</p><input value={issue} onChange={e=>setIssue(e.target.value)} placeholder="Qué debe revisarse" className="mt-4 w-full rounded-xl border p-3 text-sm"/><textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Describe el problema" rows={4} className="mt-2 w-full rounded-xl border p-3 text-sm"/><button disabled={busy} onClick={request} className="mt-3 w-full rounded-xl bg-indigo-600 text-white py-3 text-sm font-black">{busy?'Registrando…':'Enviar solicitud de revisita'}</button><button onClick={()=>setSelected(null)} className="mt-2 w-full py-2 text-xs font-bold text-slate-500">Cancelar</button></div></div>}</div></div>};