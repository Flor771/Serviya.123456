import React, { useEffect, useState } from 'react';
import { Camera, CheckCircle, MessageSquare, ShieldCheck, FileCheck2, Play, CircleCheckBig, LockKeyhole } from 'lucide-react';
import { api } from '../services/api';
import { useAuth } from '../context/AuthContext';
import { Service } from '../types';
import { WorkPhotosModal } from './WorkPhotosModal';
import { NegotiationPanel } from './NegotiationPanel';
import { WorkStatusPanel } from './WorkStatusPanel';

interface Props { service: Service; onRefresh: () => void; onOpenChat: (serviceId: string, receiverId: string) => void; }
export const WorkFlowActions: React.FC<Props> = ({ service, onRefresh, onOpenChat }) => {
  const { user } = useAuth();
  const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const [showPhotos, setShowPhotos] = useState(false); const [completion, setCompletion] = useState<any>(null); const [summary, setSummary] = useState(''); const [warranty, setWarranty] = useState<any>(null);
  const isWorker = user?.id === service.worker_id; const isClient = user?.id === service.client_id;
  const load = async () => { try { setCompletion(await api.get(`/completion/${service.id}`)); } catch { setCompletion(null); } try { const w:any = await api.get(`/completion/${service.id}/warranty`); setWarranty(w?.warranty || null); } catch { setWarranty(null); } };
  useEffect(() => { if(user) load(); }, [service.id, service.status, user?.id]);
  const executeWork = async () => { if (!window.confirm('¿Confirmas que vas a iniciar oficialmente este trabajo? La Custodia permanecerá protegida.')) return; setBusy(true); setError(''); setSuccess(''); try { await api.post(`/completion/${service.id}/execute`); setSuccess('Trabajo ejecutado correctamente. El servicio está ahora EN PROGRESO.'); await load(); onRefresh(); } catch(e:any) { setError(e?.message || 'No se pudo ejecutar el trabajo.'); } finally { setBusy(false); } };
  const submitCompletion = async () => { if(!summary.trim()){setError('Escribe un resumen de lo realizado antes de enviarlo a revisión.');return;} setBusy(true);setError('');setSuccess(''); try{await api.post(`/completion/${service.id}/submit`,{summary:summary.trim()});setSuccess('Trabajo enviado a revisión. El cliente debe comprobar la evidencia y aprobar la liberación.');await load();onRefresh();}catch(e:any){setError(e.message||'No se pudo enviar el trabajo a revisión.')}finally{setBusy(false)} };
  const approveCompletion = async () => { if(!window.confirm('¿Confirmas que revisaste el trabajo y estás conforme? Los fondos seguirán protegidos hasta que Administración procese la liberación.')) return; setBusy(true); setError(''); setSuccess(''); try { await api.post(`/completion/${service.id}/approve`); setSuccess('Sí, confirmé el trabajo. La solicitud de liberación fue enviada a Administración.'); await load(); onRefresh(); } catch(e:any) { setError(e.message || 'No se pudo registrar la aprobación.'); } finally { setBusy(false); } };
  if(!user||!service.worker_id||(!isWorker&&!isClient)) return null;
  const selected=service.status==='TRABAJADOR_SELECCIONADO'; const inProgress=service.status==='EN_PROGRESO'; const completed=service.status==='COMPLETADA';
  return <>
    <div className="fixed bottom-20 md:bottom-6 left-1/2 -translate-x-1/2 z-[45] w-[calc(100%-1.5rem)] max-w-2xl"><div className="bg-white/95 backdrop-blur-md border border-slate-200 shadow-2xl rounded-2xl p-3">
      {error&&<div className="mb-2 rounded-xl bg-red-50 border border-red-200 text-red-700 text-xs font-semibold p-2.5">{error}</div>}{success&&<div className="mb-2 rounded-xl bg-emerald-50 border border-emerald-200 text-emerald-800 text-xs font-semibold p-2.5">{success}</div>}
      <div className="flex items-center justify-between gap-3 mb-2"><div><p className="text-[10px] uppercase tracking-widest font-black text-blue-600">Flujo SERVIYA</p><p className="text-sm font-black text-slate-900">{service.status.replaceAll('_',' ')}</p></div><div className="text-[10px] text-slate-500 text-right">Publicación → selección → negociación → custodia → progreso → evidencia → conformidad → garantía</div></div>
      <WorkStatusPanel service={service}/>
      {selected&&<div className="space-y-3 mt-3"><NegotiationPanel serviceId={service.id} clientId={service.client_id} workerId={service.worker_id} budget={service.price_rd} title={service.title} onRefresh={onRefresh} onOpenChat={onOpenChat}/>{isWorker&&<button onClick={executeWork} disabled={busy} className="w-full inline-flex items-center justify-center gap-2 bg-emerald-600 hover:bg-emerald-700 text-white font-black text-sm px-4 py-3 rounded-xl disabled:opacity-50"><Play className="w-4 h-4"/>{busy?'Ejecutando…':'Ejecutar trabajo'}</button>}</div>}
      {inProgress&&<div className="space-y-2 mt-3"><div className="rounded-xl bg-blue-50 border border-blue-200 p-3 text-xs text-blue-900"><b>Custodia retenida.</b> El pago está protegido. {isWorker?'Ejecuta el trabajo y comparte evidencia.':'Revisa el avance y la evidencia del técnico.'}</div><div className="flex flex-wrap gap-2">{isWorker&&<><button onClick={()=>setShowPhotos(true)} disabled={busy} className="inline-flex items-center gap-1.5 bg-emerald-600 hover:bg-emerald-700 text-white font-bold text-xs px-3 py-2.5 rounded-xl disabled:opacity-50"><Camera className="w-4 h-4"/>Subir fotos del trabajo</button><button onClick={submitCompletion} disabled={busy} className="inline-flex items-center gap-1.5 bg-slate-900 hover:bg-slate-800 text-white font-bold text-xs px-3 py-2.5 rounded-xl disabled:opacity-50"><FileCheck2 className="w-4 h-4"/>Enviar trabajo a revisión</button></>}{isClient&&completion?.completion_submitted&&completion?.status!=='PENDIENTE_APROBACION'&&<button onClick={approveCompletion} disabled={busy} className="inline-flex items-center gap-1.5 bg-emerald-600 hover:bg-emerald-700 text-white font-black text-xs px-3 py-2.5 rounded-xl disabled:opacity-50"><CheckCircle className="w-4 h-4"/>Sí, confirmé el trabajo — solicitar liberación</button>}<button onClick={()=>onOpenChat(service.id,isWorker?service.client_id:service.worker_id!)} className="inline-flex items-center gap-1.5 bg-blue-600 hover:bg-blue-700 text-white font-bold text-xs px-3 py-2.5 rounded-xl"><MessageSquare className="w-4 h-4"/>Mensajes</button></div>{isWorker&&!completion?.completion_submitted&&<textarea value={summary} onChange={e=>setSummary(e.target.value)} placeholder="Resumen del trabajo realizado para el acta de conformidad..." className="w-full border border-slate-300 rounded-xl p-2.5 text-xs min-h-16"/>}{completion?.completion_submitted&&<div className="rounded-xl bg-amber-50 border border-amber-200 p-3 text-xs text-amber-900"><b>Trabajo enviado a revisión.</b> {isClient?(completion?.status==='PENDIENTE_APROBACION'?'Tu aprobación ya fue registrada. Administración debe liberar los fondos.':'Revisa las fotos y, cuando estés conforme, usa “Sí, confirmé el trabajo — solicitar liberación”.'):'Esperando la conformidad del cliente y la liberación de fondos.'}</div>}</div>}
      {completed&&<div className="rounded-2xl bg-emerald-50 border-2 border-emerald-300 p-4 sm:p-5 space-y-3 mt-3"><div className="flex items-center gap-3"><div className="w-11 h-11 rounded-full bg-emerald-600 text-white flex items-center justify-center"><CircleCheckBig className="w-6 h-6"/></div><div><p className="text-[10px] uppercase tracking-widest font-black text-emerald-700">SERVIYA • ESTADO FINAL</p><h3 className="text-lg sm:text-xl font-black text-emerald-950">PROCESO FINALIZADO</h3><p className="text-xs text-emerald-800">El trabajo fue aprobado y Administración liberó los fondos.</p></div></div><div className="grid grid-cols-1 sm:grid-cols-3 gap-2"><div className="rounded-xl bg-white/80 border border-emerald-200 p-3"><p className="text-[10px] uppercase font-black text-slate-400">Cliente</p><p className="text-xs font-bold text-slate-800 mt-1">Conformidad registrada</p></div><div className="rounded-xl bg-white/80 border border-emerald-200 p-3"><p className="text-[10px] uppercase font-black text-slate-400">Trabajador</p><p className="text-xs font-bold text-slate-800 mt-1">Pago liberado a su billetera</p></div><div className="rounded-xl bg-white/80 border border-emerald-200 p-3"><p className="text-[10px] uppercase font-black text-slate-400">Custodia</p><p className="text-xs font-bold text-slate-800 mt-1">Fondos liberados</p></div></div><div className="rounded-xl bg-white border border-emerald-200 p-3 text-xs text-emerald-900 flex items-center gap-2"><LockKeyhole className="w-4 h-4 shrink-0"/><span><b>Proceso cerrado.</b> Esta confirmación la pueden ver tanto el Cliente como el Trabajador/Técnico.</span></div></div>}
      {warranty&&<div className="mt-2 rounded-xl bg-emerald-50 border border-emerald-200 p-3 text-xs text-emerald-900 flex items-start gap-2"><ShieldCheck className="w-4 h-4 shrink-0"/><div><b>Garantía SERVIYA activa por {warranty.coverage_days} días.</b><br/>Certificado: {warranty.certificate_ref} • Vence: {new Date(warranty.expires_at).toLocaleDateString()}</div></div>}
    </div></div>
    {showPhotos&&isWorker&&<WorkPhotosModal service={service} onClose={()=>setShowPhotos(false)} onRefresh={onRefresh}/>} 
  </>;
};
