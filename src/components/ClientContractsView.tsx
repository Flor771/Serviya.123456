import React, { useEffect, useMemo, useState } from 'react';
import { MessageSquare, ShieldCheck, Clock3, CheckCircle2, AlertTriangle, ChevronRight, FileCheck2, Fingerprint } from 'lucide-react';
import { Service } from '../types';
import { api } from '../services/api';
interface Props { services: Service[]; onOpenService: (service: Service) => void; onOpenChat: (serviceId: string, workerId: string) => void; }
interface DigitalContract { id:string; service_id:string; contract_number:string|null; status:string; content_hash:string|null; generated_at:string|null; client_accepted_at:string|null; worker_accepted_at:string|null; escrow_status:string; }
const labels: Record<string,string> = { TRABAJADOR_SELECCIONADO:'Técnico seleccionado', EN_PROGRESO:'Trabajo en progreso', COMPLETADA:'Trabajo terminado', EN_DISPUTA:'En disputa', RECIBIENDO_POSTULACIONES:'Recibiendo propuestas', PUBLICADA:'Publicada' };
const tone: Record<string,string> = { TRABAJADOR_SELECCIONADO:'bg-blue-50 text-blue-700', EN_PROGRESO:'bg-amber-50 text-amber-700', COMPLETADA:'bg-emerald-50 text-emerald-700', EN_DISPUTA:'bg-red-50 text-red-700' };
export const ClientContractsView: React.FC<Props> = ({ services, onOpenService, onOpenChat }) => {
 const contracts = useMemo(() => services.filter(s => Boolean(s.worker_id)), [services]); const active = contracts.filter(s => ['TRABAJADOR_SELECCIONADO','EN_PROGRESO','EN_DISPUTA'].includes(String(s.status))); const finished = contracts.filter(s => String(s.status) === 'COMPLETADA');
 const [digital,setDigital] = useState<Record<string,DigitalContract>>({}); const [selected,setSelected] = useState<any>(null); const [accepting,setAccepting] = useState(false); const [message,setMessage] = useState('');
 useEffect(()=>{api.get<{contracts:DigitalContract[]}>('/contracts').then(r=>{const map:Record<string,DigitalContract>={};(r.contracts||[]).forEach(c=>{map[c.service_id]=c});setDigital(map)}).catch(()=>setDigital({}))},[services]);
 const openContract=async(serviceId:string)=>{try{setMessage('');setSelected(await api.get<any>(`/contracts/contract-${serviceId}`))}catch(e:any){setMessage(e?.message||'El contrato todavía no está disponible.')}};
 const acceptContract=async()=>{if(!selected?.contract?.id)return;setAccepting(true);setMessage('');try{const r=await api.post<any>(`/contracts/${selected.contract.id}/accept`,{});setMessage(`Firma electrónica registrada. Huella de aceptación: ${r.acceptance_hash}`);const c=await api.get<{contracts:DigitalContract[]}>('/contracts');const map:Record<string,DigitalContract>={};(c.contracts||[]).forEach(x=>{map[x.service_id]=x});setDigital(map);setSelected(await api.get<any>(`/contracts/${selected.contract.id}`))}catch(e:any){setMessage(e?.message||'No se pudo registrar la firma.')}finally{setAccepting(false)}};
 return <section className="space-y-5"><div className="bg-white rounded-3xl p-5 sm:p-6 border border-slate-200 shadow-sm"><div className="flex items-start gap-3"><div className="w-11 h-11 rounded-2xl bg-blue-50 text-blue-600 flex items-center justify-center shrink-0"><ShieldCheck className="w-5 h-5"/></div><div><p className="text-blue-700 text-xs font-black uppercase tracking-wide">CLIENTE</p><h1 className="text-2xl font-black text-slate-900 mt-1">Mis contrataciones</h1><p className="text-sm text-slate-500 mt-1 leading-relaxed">Aquí aparecen tus contrataciones y, después de verificar el depósito, el contrato digital que deja constancia del acuerdo.</p></div></div><div className="grid grid-cols-2 gap-2 mt-4"><div className="rounded-xl bg-blue-50 p-3 text-center"><p className="text-lg font-black text-blue-700">{active.length}</p><p className="text-[10px] font-bold text-blue-700">Activas</p></div><div className="rounded-xl bg-emerald-50 p-3 text-center"><p className="text-lg font-black text-emerald-700">{finished.length}</p><p className="text-[10px] font-bold text-emerald-700">Terminadas</p></div></div></div>{message&&<div className="bg-blue-50 border border-blue-200 text-blue-800 rounded-2xl p-3 text-xs font-semibold break-all">{message}</div>}{!contracts.length&&<div className="bg-white rounded-2xl border border-slate-200 p-8 text-center"><Clock3 className="w-9 h-9 mx-auto text-slate-300"/><p className="font-black text-slate-700 mt-3">Todavía no tienes contrataciones</p></div>}<div className="grid grid-cols-1 md:grid-cols-2 gap-4">{contracts.map(s=>{const status=String(s.status);const price=Number(s.negotiated_price_rd||s.price_rd||0);const c=digital[s.id];return <article key={s.id} className="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden"><button onClick={()=>onOpenService(s)} className="w-full text-left p-5 hover:bg-slate-50 transition"><div className="flex items-start gap-3"><div className={`w-10 h-10 rounded-xl flex items-center justify-center shrink-0 ${status==='COMPLETADA'?'bg-emerald-50 text-emerald-600':status==='EN_DISPUTA'?'bg-red-50 text-red-600':'bg-blue-50 text-blue-600'}`}>{status==='COMPLETADA'?<CheckCircle2 className="w-5 h-5"/>:status==='EN_DISPUTA'?<AlertTriangle className="w-5 h-5"/>:<Clock3 className="w-5 h-5"/>}</div><div className="min-w-0 flex-1"><p className="text-[10px] font-black uppercase tracking-wide text-blue-600">{s.category_name}</p><h2 className="font-black text-base text-slate-900 mt-0.5 break-words">{s.title}</h2><p className="text-sm text-slate-500 mt-2">Técnico: <span className="font-bold text-slate-700">{s.worker_name||'Técnico asignado'}</span></p><div className="flex flex-wrap gap-2 mt-3 text-[10px] font-black"><span className={`px-2 py-1 rounded-lg ${tone[status]||'bg-slate-100 text-slate-600'}`}>{labels[status]||status}</span><span className="px-2 py-1 rounded-lg bg-slate-100 text-slate-600">RD$ {price.toLocaleString()}</span></div></div><ChevronRight className="w-5 h-5 text-slate-400 shrink-0 mt-1"/></div></button><div className="px-5 pb-5 space-y-2"><div className="rounded-xl border border-emerald-100 bg-emerald-50 p-3 text-xs text-emerald-800 font-semibold">{status==='TRABAJADOR_SELECCIONADO'?'Técnico seleccionado. Completa el pago para poner los fondos en Custodia SERVIYA.':status==='EN_PROGRESO'?'Trabajo activo. Puedes coordinar con el técnico y revisar el avance.':status==='COMPLETADA'?'Trabajo terminado. Conserva el contrato digital y comprobante como evidencia.':status==='EN_DISPUTA'?'Este trabajo está en disputa y requiere revisión.':'Seguimiento de la contratación.'}</div>{c?.contract_number&&<button onClick={()=>openContract(s.id)} className="w-full inline-flex items-center justify-center gap-2 py-2.5 rounded-xl bg-indigo-50 border border-indigo-200 text-indigo-700 text-xs font-black"><FileCheck2 className="w-4 h-4"/> Contrato digital · {c.status}</button>}{s.worker_id&&<button onClick={()=>onOpenChat(s.id,s.worker_id!)} className="w-full inline-flex items-center justify-center gap-2 py-2.5 rounded-xl bg-blue-50 border border-blue-200 text-blue-700 text-xs font-black"><MessageSquare className="w-4 h-4"/> Mensaje al técnico</button>}</div></article>})}</div>{selected&&<div className="fixed inset-0 z-[70] bg-slate-950/80 backdrop-blur-sm p-4 overflow-y-auto"><div className="min-h-full flex items-center justify-center"><div className="bg-white rounded-3xl max-w-3xl w-full shadow-2xl my-6 overflow-hidden"><div className="bg-slate-900 text-white p-6"><div className="flex items-center gap-2 text-emerald-400"><FileCheck2 className="w-5 h-5"/><span className="text-xs font-black tracking-widest">SERVIYA · CONTRATO DIGITAL</span></div><h2 className="text-xl sm:text-2xl font-black mt-2">{selected.contract.contract_number}</h2><p className="text-xs text-slate-400 mt-1">Documento inmutable · Huella SHA-256</p></div><div className="p-6 space-y-4"><div className="grid grid-cols-1 sm:grid-cols-2 gap-3 text-sm"><div className="p-3 rounded-xl bg-slate-50"><span className="text-[10px] uppercase text-slate-400 font-bold">Estado</span><p className="font-black mt-1">{selected.contract.status}</p></div><div className="p-3 rounded-xl bg-slate-50"><span className="text-[10px] uppercase text-slate-400 font-bold">Emitido</span><p className="font-black mt-1">{selected.contract.generated_at||'—'}</p></div></div><div className="p-3 rounded-xl bg-indigo-50 border border-indigo-100 break-all"><div className="flex items-center gap-2 text-indigo-700"><Fingerprint className="w-4 h-4"/><span className="text-[10px] font-black uppercase">Integridad</span></div><p className="text-xs font-mono mt-1">{selected.contract.content_hash}</p></div><div className="border rounded-2xl p-4 max-h-80 overflow-y-auto text-xs text-slate-700"><pre className="whitespace-pre-wrap font-sans">{JSON.stringify(selected.document,null,2)}</pre></div><div className="text-[11px] text-slate-500 bg-amber-50 border border-amber-100 rounded-xl p-3">El contrato registra el acuerdo, el depósito en Custodia, las partes, el precio y la trazabilidad. Su fuerza legal frente a terceros depende de la legislación aplicable y de las formalidades que correspondan.</div><div className="flex flex-col sm:flex-row gap-2"><button disabled={accepting||Boolean(selected.contract.client_accepted_at)} onClick={acceptContract} className="flex-1 py-3 rounded-xl bg-indigo-600 text-white text-xs font-black disabled:opacity-50">{selected.contract.client_accepted_at?'✓ Aceptado y firmado por ti':accepting?'Registrando firma…':'Aceptar y firmar contrato'}</button><button onClick={()=>setSelected(null)} className="py-3 px-5 rounded-xl bg-slate-100 text-slate-700 text-xs font-black">Cerrar</button></div></div></div></div></div>}</section>;
};